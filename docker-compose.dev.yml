services:
  presentation:
    image: node:22-alpine
    working_dir: /app
    command: ["node", "dev-server.mjs"]
    ports:
      - "8080:8080"
    environment:
      PORT: "8080"
    volumes:
      - .:/app:ro
    restart: unless-stopped
